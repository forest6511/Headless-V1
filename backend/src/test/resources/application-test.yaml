spring:
  application:
    name: headblog-test

  datasource:
    url: jdbc:postgresql://localhost:5432/headblog-test
    username: headblog
    password: headblog
    driver-class-name: org.postgresql.Driver
    hikari:
      auto-commit: false
      maximum-pool-size: 5
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1200000

jwt:
  secret: "U2FsdGVkX1+qv3Rj91C3jKM3jsRPT8JMKZ0wv3RPkxGkv0aF2JRvs9JTPX8="
  expiration: 1800000 # 30分（ミリ秒単位で設定）

logging:
  level:
    org.flywaydb: DEBUG
    org.jooq: DEBUG

server:
  port: 8081
